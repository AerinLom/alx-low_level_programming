#include "main.h"
/**
  *mul - function that multiplies numbers
  *@a: first number to be multiplied
  *@b: second number to be multiplied
  *Return: product of integers (Success)
  */
int mul(int a, int b)
{
	return (a * b);
}
